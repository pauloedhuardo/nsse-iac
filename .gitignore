# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
errored.tfstate
crash.log
crash.*.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Node
node_modules/
*.zip

# Credenciais
*.pem
!**/documentdb-ca.pem

# Regenerado por `terraform output -json` a cada execucao do Ansible.
# Contem a chave privada do key pair EC2, entao nao pode ser versionado.
terraform/server/outputs.json

terraform/serverless-minimal
